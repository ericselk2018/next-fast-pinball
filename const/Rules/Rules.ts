export const startingBallsPerPlayer = 4;

// Just for doing dev work with UI layout/styles really, normally will be 0.
export const startingScore = 8888888888;

// Will probably always be true, but could be false for an easy mode option.
export const clearProgressWhenNoBallsInPlay = false;
