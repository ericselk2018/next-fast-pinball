// Only used while doing dev work - to force virtual hardware
// TODO: set this with environment variable and npm run dev sets, npm run start does not
export const useVirtualHardware = false;

// Only used while doing dev work - for game to start, normally set to 0.
export const autoStartGamePlayers = 0;

// Only used while doing dev work - to skip mode select, normally set to 0.
export const autoStartBallsInPlay = 0;

// The number of balls in the machine.
export const totalBallsInMachine = 1;

// Only used while doing dev work - to skip adding credits, normally set to 0.
// My coin door isn't wired up yet :(
export const startingCredits = 10;
